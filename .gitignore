# Dependencies
node_modules/

# Next.js build output
.next/
out/

# Environment files — never commit real credentials
.env
.env.local
.env.*.local

# Prisma
prisma/migrations/

# Generated SQL dumps — regenerate with `npx tsx scripts/dump-sql.ts`.
# Large, and the article rows already live in the committed data/*.json
# snapshots, so committing these would just duplicate them.
dump/

# Claude Code local settings (may contain credentials)
.claude/settings.local.json

# Editor / OS
.DS_Store
*.pem
Thumbs.db

.vercel
.env*.local
.env.vercel
